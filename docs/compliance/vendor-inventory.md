# Vendor Inventory

| service | purpose | data shared | secrets used | production criticality | fallback behavior | current health/probe status | contract/SLA status placeholder | security review status placeholder | replacement/exit notes |
|---|---|---|---|---|---|---|---|---|---|
| Clerk | Authentication/session identity. | User email, auth identifiers, session/JWT metadata, MFA state. | Clerk secret key, JWKS/issuer config. | High. Console access depends on auth. | Mock auth only allowed in dev/local/test; production fails closed if mock auth is configured. | Auth provider shown in Provider Health summary; local app stores approval/role separately. | TBD | TBD | Alternative auth provider would need JWT verification and identity reconciliation migration. |
| OpenAI | Explanation-only recommendation summaries and Opportunity Intelligence. | Prompt context containing market/recommendation/risk summaries; no secrets intentionally sent. | `OPENAI_API_KEY` / legacy `LLM_API_KEY`. | Medium. Deterministic workflows continue with mock fallback. | Schema validation and deterministic mock explanation fallback. | Provider Health separates config/probe state and redacts errors/secrets. | TBD | TBD | Replace with another schema-validating LLM provider or disable explanations. |
| Massive/Polygon | Provider-backed market data. | Requested symbols/timeframes; no user PII needed for market data calls. | Polygon/Massive API key. | High for provider-backed workflow trust. | Fallback chart reads; workflow endpoints block hidden fallback unless dev/local/test override is explicit. | Provider Health live probe and source labels. | TBD | TBD | Replace through market data provider abstraction. |
| Resend | Invite, approval, rejection, and strategy-report email delivery. | Recipient email, display name, email body, report content. | Resend API key. | Medium. Core console can run without email delivery, but onboarding/reporting degrades. | Console/mock email mode for local/dev. | Email provider mode shown in summaries; email logs persist delivery status. | TBD | TBD | Replace through email provider abstraction. |
| Alpaca | Paper broker readiness/config scaffold and alternate market data scaffold. | Potential symbols/order intent if enabled in future; current broker mode defaults to mock. | Alpaca API key/secret. | Low in current scope. No live broker routing. | `BROKER_PROVIDER=mock` keeps deterministic mock broker; Provider Health wording states readiness only. | Provider Health config/readiness only; no live routing activation. | TBD | TBD | Remove or replace broker adapter before any execution scope change. |
| FRED | Future macro calendar/data readiness. | Macro data requests; no user PII. | FRED API key. | Low/Medium. Current macro provider can remain mock/static. | Mock/static macro calendar provider. | Provider Health reports config readiness, not a live probe. | TBD | TBD | Replace through macro calendar provider abstraction. |
| Cloudflare/Caddy edge stack | Tunnel/reverse proxy/TLS/HSTS/access routing. | HTTP request metadata, headers, TLS termination data depending on deployment. | Cloudflare tunnel/access credentials, Caddy config secrets if any. | High for deployed access. | Local localhost dev servers. | Deployment docs describe tunnel/proxy; app headers supplement edge controls. | TBD | TBD | Direct hosting or another reverse proxy would require DNS/TLS/session review. |
| GitHub | Source control, issue/PR/release history. | Source code, docs, tests, non-secret config. | Git credentials/tokens outside repo. | High for SDLC evidence. | Local git history if GitHub unavailable. | Git status/branch/commit captured in release evidence. | TBD | TBD | Mirror repository and export release artifacts. |
| Codex/Claude tooling | Assisted development, code review, docs generation, local worktrees. | Prompts, code snippets, local context chosen by operator. | Tool credentials outside repo. | Medium for development velocity, not runtime. | Manual engineering workflow. | Deployment excludes `.claude`, `.tmp`, and generated artifacts. | TBD | TBD | Keep human-reviewed diffs and avoid storing secrets in prompts/artifacts. |
