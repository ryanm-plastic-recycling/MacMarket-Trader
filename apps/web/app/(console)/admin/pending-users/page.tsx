import { ConsolePage } from "@/components/console-page";

export default function Page() {
  return <ConsolePage title="admin pending users" subtitle="Admin-only queue for approval/rejection decisions with email notification logging." />;
}
