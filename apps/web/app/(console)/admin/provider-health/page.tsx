import { ConsolePage } from "@/components/console-page";

export default function Page() {
  return <ConsolePage title="provider health" subtitle="Operational heartbeat across auth, market-data, and email provider boundaries." />;
}
