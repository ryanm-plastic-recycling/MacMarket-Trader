import { ConsolePage } from "@/components/console-page";

export default function Page() {
  return <ConsolePage title="replay runs" subtitle="Paper-only replay runs with per-step pre/post portfolio snapshots." />;
}
